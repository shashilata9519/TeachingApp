import { withNavigation } from "react-navigation"
import { Text } from "react-native"

function CourseApplyWithTeacherScreen(props) {
    return <Text>Course Apply with Teacher Screen</Text>
}

export default withNavigation(CourseApplyWithTeacherScreen)